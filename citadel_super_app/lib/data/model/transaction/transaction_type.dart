enum TransactionType {
  dividend,
  placement,
}

enum TransactionStatus {
  completed,
  pending,
  failed,
  success,
  cancelled,
  expired,
  invalid,
  unknown,
  open,
  reversed,
  processing,
  inReview,
  draft,
  active,
  approved,
  rejected
}
